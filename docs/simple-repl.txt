-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


@package simple-repl
@version 0.1.0.0


-- | Simple REPL library.
module Repl

-- | Representation of a REPL.
data Repl
Repl :: IO Text -> (Text -> Text) -> (Text -> IO ()) -> Text -> Repl

-- | Command used to read the input from the terminal.
[replRead] :: Repl -> IO Text

-- | Evaluation function.
[replEval] :: Repl -> Text -> Text

-- | Command used to print the result of evaluation.
[replPrint] :: Repl -> Text -> IO ()

-- | Used to check if REPL should break the loop.
[replBreak] :: Repl -> Text

-- | This is the default REPL, echoes all input. Use <tt>:quit</tt> to
--   break the loop.
defaultRepl :: Repl

-- | Starts the REPL.
runRepl :: Repl -> IO ()
